# FastAPI and Web Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database and ORM
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Machine Learning
scikit-learn==1.4.0
xgboost==2.0.3
prophet==1.1.5
# lightfm is optional; pinning/build can fail on some platforms.
# Install separately if needed: pip install "lightfm==1.17" --no-use-pep517
pandas==2.2.0
numpy==1.26.3
pyarrow==14.0.2

# ML Operations
mlflow==2.10.1
# prefect is optional; pinned version conflicts with FastAPI/Starlette pins.
# Install separately if you need orchestration features.
# prefect==2.14.21
# feast is optional; recent versions require numpy>=2 which conflicts with pinned numpy.
# Install separately if you need feature store integration.
# feast[postgres]==0.59.0

# Data Validation
great-expectations==0.18.8

# API and HTTP
httpx==0.26.0
requests==2.31.0

# Configuration and Environment
python-dotenv==1.0.0

# Monitoring and Logging
structlog==24.1.0
prometheus-client==0.19.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Code Quality
black==24.1.1
ruff==0.1.11
mypy==1.8.0

# Utilities
python-dateutil==2.8.2
pytz==2023.4

