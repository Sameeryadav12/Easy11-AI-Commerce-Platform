/**
 * Shipping thresholds and copy used across storefront, cart, and checkout.
 * Keeps "Free standard shipping over $X" consistent.
 */
export const FREE_STANDARD_SHIPPING_THRESHOLD = 50;

/** Human-readable copy for standard free shipping. */
export const FREE_STANDARD_SHIPPING_LABEL = `Free on orders over $${FREE_STANDARD_SHIPPING_THRESHOLD}`;
